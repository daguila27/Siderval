<%
function fechaLatino(string){
    if(string.split('-').length == 3){
        return [string.split('-')[2],string.split('-')[1],string.split('-')[0]].join('-');
    }
    else{
        return [string.split('/')[1],string.split('/')[0],string.split('/')[2]].join('/');
    }
}

function completarDigitos(num, dig){
    var arr = [];
    if(num.toString().length >= dig){
        return num.toString();
    }
    else{
        for(var w=0; w < dig-num.toString().length; w++){arr.push(0);}
        return arr.join('')+num.toString();
    }
}
%>
<!-- Oculta cantidad de datos -->
<style>
    .hiddenCol{
        display: none;
    }
</style>
<div id="registros" style="display: none"></div>

<table id="rechTable" class="o_list_view table table-condensed table-striped o_list_view_ungrouped" style="width: 100%; margin-top: -2px !important;">
    <thead>
    <tr>
        <th colspan="1" data-toggle="tooltip" data-placement="bottom" title="Código Único de Colada" style="text-align: center; white-space: nowrap;">Código Único de Colada</th>
        <th colspan="1" data-toggle="tooltip" data-placement="bottom" title="Total Rechazados" style="text-align: center; white-space: nowrap; ">Total Rechazados</th>
        <th colspan="1" data-toggle="tooltip" data-placement="bottom" title="Peso Total" style="text-align: center; white-space: nowrap; ">Peso Total [kg]</th>
    </tr>
    </thead>
    <tbody id="rechTablebody">
    <%
    var pesoT = 0;
    if(datalen.length){
    for(var i=0; i < datalen.length; i++){%>
    <tr class="column-prods" style="background-color: rgb(239,239,248) !important;" onclick="$('.hiddenCol<%= i%>').toggleClass('hiddenCol'); $(this).find('i.glyphicon').toggleClass('glyphicon-chevron-down glyphicon-chevron-up');">
        <td colspan="1" data-order="<%=datalen[i].colada%>-<%=i%>" style="text-align: left;"><b><i class="glyphicon glyphicon-chevron-down"></i> <%=datalen[i].colada %></b></td>
        <td colspan="1" data-order="<%=datalen[i].enviadosTotal%>-<%=i%>" style="text-align: center;white-space: nowrap;font-weight: bold;" class="parsear_nro" data-cs="1" data-valor="<%=datalen[i].enviadosTotal %>"><%= datalen[i].enviadosTotal %></td>
        <td colspan="1" data-order="<%=datalen[i].pesoTotal%>-<%=i%>" style="text-align: center;white-space: nowrap;font-weight: bold;" class="parsear_nro" data-cs="1" data-valor="<%=datalen[i].pesoTotal %>"><%= datalen[i].pesoTotal %></td>
        <% pesoT += datalen[i].peso;%>
    </tr>
    <%for(var t=0; t < datalen[i].det_rech.split('%').length; t++){%>
    <%if( t===0 ){%>
        <tr class="column-prods hiddenCol hiddenCol<%= i%>" style="background-color: rgb(238,238,238); color: rgb(76,76,112);">
            <th data-toggle="tooltip" data-placement="bottom" title="Etapa de Rechazo" style="text-align: center; white-space: nowrap; ">Etapa de Rechazo</th>
            <th data-toggle="tooltip" data-placement="bottom" title="Cantidad Rechazada" style="text-align: center; white-space: nowrap; ">Cantidad Rechazada</th>
            <th data-toggle="tooltip" data-placement="bottom" title="Peso Unitario" style="text-align: center; white-space: nowrap; ">Peso Unitario [kg]</th>
        </tr>
    <%}%>
        <tr class="column-prods hiddenCol hiddenCol<%= i%>" style="background-color: #f2dede;">
            <td colspan="1" data-order="<%=datalen[i].colada%>-<%=i%>"><%=datalen[i].det_rech.split('%')[t].split('@')[0]%> - <%=datalen[i].det_rech.split('%')[t].split('@')[3]%></td>
            <td colspan="1" data-order="<%=datalen[i].enviadosTotal%>-<%=i%>" style="text-align: center"><%=datalen[i].det_rech.split('%')[t].split('@')[1]%></td>
            <td colspan="1" data-order="<%=datalen[i].pesoTotal%>-<%=i%>" style="text-align: center"><%=datalen[i].det_rech.split('%')[t].split('@')[2]%></td>
        </tr>
    <%}%>
    <%}
    } else { %>
        <tr>
            <td colspan="3">
                <h2 style="text-align: center"><small> <i class="fa fa-exclamation-circle"></i> No hay nada por producir</small></h2>
            </td>
            <td colspan="0"></td>
            <td colspan="0"></td>
        </tr>
    <%}%>
    </tbody>
    <tfoot id="rechTablefooter">
    <tr>
        <td colspan="1"></td>
        <td colspan="1"></td>
        <td colspan="1" class="footer-pesoT parsear_nro" cs="2" style="display: flex; align-content: center; width: 100%"><%= pesoT%></td>
    </tr>
    </tfoot>
</table>

<!-- Funcion para parsear nros -->
<script src="/assets/parsear_nro.js" type="text/javascript"></script>
<script type="text/javascript">
    //setDataTableFixed("#rechTable");
    $(document).ready(function(){
        busc.initFixed('main-page', true);
    });
</script>
